/**
 * Cuenta la cantidad de formas diferentes en que se puede dar cambio
 * para una cantidad de dinero dada, utilizando una matriz de denominaciones de monedas.
 * @param amount La cantidad de dinero a cambiar.
 * @param denominations Una matriz de denominaciones de monedas únicas.
 * @returns La cantidad de formas diferentes en que se puede dar cambio.
 */
export function countChange(amount: number, denominations: number[]): number {
  // implementación de la función
  return 1
}
